#include <math.h>
//function that cubes index+1

int sumOfCube(int n, int i) {
    if(n<1) {
        return i;
    }
    return sumOfCube(n-1, i+1);
}

int sumOfCube(int n) {
    if
    return pow(n,3);
}
