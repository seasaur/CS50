#include <math.h>
//function that cubes index+1

int sumOfCube(int n, int i) {
    if(i==n) {
        return i;
    }
    return sumOfCube(n, i+1);
}

int sumOfCube(int n) {
    if
    return pow(n,3);
}
